import 'package:flutter/material.dart';

class RadiusConstants {
  static const BorderRadius buttonRadius = BorderRadius.all(Radius.circular(25));
  static const BorderRadius largeBtnRadius = BorderRadius.all(Radius.circular(50));

}
